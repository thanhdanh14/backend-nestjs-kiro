// Enum định nghĩa các roles trong hệ thống
export enum Role {
  USER = 'user',       // User thường
  ADMIN = 'admin',     // Admin
  MODERATOR = 'moderator', // Moderator (người kiểm duyệt)
}

// Có thể thêm nhiều roles khác:
// SUPER_ADMIN = 'super_admin'
// EDITOR = 'editor'
// VIEWER = 'viewer'
